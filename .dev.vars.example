# Local secrets for `opennextjs-cloudflare preview` / `wrangler dev`.
# Copy to `.dev.vars` (gitignored) and fill in real values.
# In production these are set as Cloudflare *Secrets*, NOT as vars in wrangler.jsonc.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
